<% form.class.human_attribute_name(field).each do |option| %>
  <%= f.check_box field, { name: "#{form.class_name}[#{field}][]", checked: form.choosed_option?(option, field) }, option, nil %>
  <%= f.label field, option %>
<% end %>
