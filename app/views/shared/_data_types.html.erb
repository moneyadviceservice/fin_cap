<ul class="evidence-types">
  <% presenter.data_type_keys.each do |data_type| %>
    <li class="evidence-types__item data-types__<%= "#{data_type.downcase}" %>">
      <%= render 'shared/evidence_types', icon: presenter.data_type_icon(data_type), text: data_type %>
    </li>
  <% end %>
</ul>
